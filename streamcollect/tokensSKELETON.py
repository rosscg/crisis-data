#Twitter API Credentials:
CONSUMER_KEY = ''
CONSUMER_SECRET = ''
ACCESS_TOKENS = (       # Format: ('screen_name', 'token', 'secret')
    ('', '', ''),       # The trailing comma must remain if only one token set is added.
    ('', '', '')        # This line can be removed if only one token set is used,
)                       # however, a minimum of three is required for the 3 stream types.

# Mapbox Credentials
MAPBOX_PK = None        # [Optional] Generate at www.mapbox.com, used for viewing
                        # mapped data. Enter here as a string contained by ''
